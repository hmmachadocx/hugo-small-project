name: AST Vscode Extension CI Windows Test cases

on: [pull_request]

jobs:
  run-scan:
    strategy:
      matrix:
        os: [windows-latest]
      fail-fast: true
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3.0.2
